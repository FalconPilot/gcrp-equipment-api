{
  "name": "gcrp-equipment",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "parcel index.html",
    "watch": "parcel watch index.html --out-dir ../src/dist --cache-dir ../src/.parcel-cache",
    "build": "parcel build index.html --out-dir ../src/dist --cache-dir ../src/.parcel-cache",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "auth0-js": "^9.10.0",
    "parcel-bundler": "^1.12.0",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-scripts": "^2.1.8",
    "@babel/plugin-proposal-class-properties": "^7.3.4"
  },
  "devDependencies": {
    "@babel/core": "^7.3.4"
  }
}
