{
  "name": "gcrp-equipment-api",
  "private": true,
  "version": "0.1.0",
  "author": "falconpilot",
  "scripts": {
    "dev": "npm run install:front && concurrently 'cd front && npm run watch' 'dotenv nodemon src/index.js'",
    "install:front": "cd front && npm install && npm run build",
    "build": "npm run install:front",
    "start": "node src/index.js"
  },
  "heroku-run-build-script": true,
  "dependencies": {
    "express": "^4.16.4",
    "fs-extra": "^7.0.1",
    "mime": "^2.4.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "dotenv-cli": "^1.4.0",
    "nodemon": "^1.18.10",
    "parcel-bundler": "^1.12.0"
  }
}
